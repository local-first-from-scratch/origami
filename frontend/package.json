{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vue-tsc --noEmit && rsbuild build",
    "check": "biome check --write",
    "check:ci": "biome ci --error-on-warnings",
    "dev": "rsbuild dev --open",
    "format": "biome format --write",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.5",
    "@rsbuild/core": "^1.3.22",
    "@rsbuild/plugin-vue": "^1.0.7",
    "rsbuild-plugin-wasmpack": "^0.0.3",
    "typescript": "^5.8.3",
    "vue-tsc": "^2.2.10"
  },
  "trustedDependencies": [
    "core-js"
  ]
}
